{
  "name": "committee-automation",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "web"
  ],
  "scripts": {
    "build": "npm run build --workspace web",
    "start": "npm run db:setup --workspace web && npm run start --workspace web",
    "test": "npm run test --workspace web"
  },
  "engines": {
    "node": ">=22"
  }
}
